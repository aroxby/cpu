language: cpp
services: docker
install: docker build -t $TRAVIS_REPO_SLUG .
script:
  - env | sort
  - docker run --rm -v $(pwd):/src:Z
    -e TRAVIS_JOB_ID=$TRAVIS_JOB_ID
    $TRAVIS_REPO_SLUG make coveralls
